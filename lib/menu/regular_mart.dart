import 'package:flutter/material.dart';

class regular_mart extends StatelessWidget {
  const regular_mart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
